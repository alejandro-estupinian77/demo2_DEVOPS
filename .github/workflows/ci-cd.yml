name: Test Pipeline

on:
  push:
    branches: [ main, master, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: 📦 Install dependencies
      run: |
        cd backend
        npm install

    - name: 🧪 Run tests
      run: |
        cd backend
        npm run test:unit
      env:
        NODE_ENV: test

    - name: ✅ Test completed
      run: echo "🎉 Pipeline funcionó correctamente!"